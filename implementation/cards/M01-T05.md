# M01-T05 — Freeze compatibility gate and minimum M02 test matrix

- milestone: M01
- execution_status: done
- executor: chatgpt
- priority: P0
- complexity: high
- phase: research/synthesis
- depends_on: [M01-T02, M01-T03, M01-T04]
- OpenSpec: no

## Goal

Convert M01 evidence into the concrete fail-closed admission gate and minimum test matrix that any M02 custom-runtime design must satisfy before substitution is allowed.

## Scope

Included: identity/provenance inputs; source-baseline relation; protocol/schema checks; patch/equivalence status; required build/test evidence; PASS/FAIL/UNKNOWN semantics; rollback/update implications; minimum M02 prerequisite matrix.

Excluded: implementing gate code, feature schema, package hooks or custom runtime.

## Authoritative inputs

T02/T03/T04 evidence; requirements R4-R9, R13-R16; Master Plan global invariants and M01 acceptance.

## Refresh Gate

Confirm all dependency evidence refers to the same intended official package/runtime baseline. If not, reconcile or block rather than synthesize mixed-baseline conclusions.

## Expected outputs

- `implementation/evidence/M01-T05_COMPATIBILITY_GATE.md`
- final M01 compatibility gate definition
- minimum acceptance/test matrix required before M02 substitution design/implementation can be accepted
- M01 integrated acceptance inputs

## Acceptance criteria

Gate defines at minimum:
- exact official package identity and bundled-runtime identity;
- custom runtime source/upstream/patch provenance;
- deterministic/safe baseline relation requirement;
- relevant app-server protocol/schema compatibility checks;
- strict wakeup-equivalence/patch behavior requirement;
- `PASS`, `FAIL`, `UNKNOWN`, with only PASS eligible for substitution;
- behavior when upstream equivalence retires the patch;
- behavior when update baseline changes;
- evidence and diagnostics required on rejection.

Minimum matrix covers at least build/provenance identity, app-server initialize/session smoke, short inline command, yielding background completion, idle wake, active-turn delivery, duplicate suppression, interactive/intermediate `write_stdin`, feature off/stock path, and update re-evaluation. It must identify which checks belong to later M02/M03/M04/M05 rather than executing them in M01.

## Required evidence/checks

Cross-check every gate input against dependency evidence and requirement coverage. Perform an internal consistency review for fail-closed and rollback semantics.

## Executor

`chatgpt_only`; executor null until start. No OpenSpec for M01 synthesis. M02 must re-evaluate OpenSpec JIT.

## Git/GitHub state

Project `main`; external repos read-only.

## Blocker/escalation

If safe compatibility cannot be decided from available identity/protocol evidence, the correct gate result is UNKNOWN and M02 custom substitution remains blocked until a stronger evidence path exists. Do not weaken R6/R7.

## Result

executor: chatgpt
result_commit: e0509a93cc2c02320a7deec042666a6de791a963
result_pr: null
evidence: implementation/evidence/M01-T05_COMPATIBILITY_GATE.md
tests_summary: Synthesized exact PASS/FAIL/UNKNOWN admission semantics, required official/candidate provenance, source/equivalence states, patch behavior, Desktop protocol smoke, feature rollback and update-refresh requirements; mapped minimum downstream checks to M02-M05 and confirmed current actual substitution remains UNKNOWN/NOT AUTHORIZED.

## Definition of Done

Gate/matrix are durable, requirement coverage is explicit, all M01 acceptance inputs are ready for integrated milestone review, and no implementation work has begun.
