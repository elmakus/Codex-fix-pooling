# M02-T01 — Verify Desktop runtime-selection coverage and freeze substitution seam

- milestone: M02
- execution_status: ready
- executor: null
- priority: P0
- complexity: high
- phase: research/design
- depends_on: []
- OpenSpec: candidate; actual OpenSpec deferred until seam decision is proven

## Goal

Verify whether the exact current Desktop/Community launch paths consistently honor `CODEX_CLI_PATH` for the Codex/app-server authority used by Desktop, then freeze the M02 substitution mechanism as either preferred staged-runtime + `CODEX_CLI_PATH` or fallback candidate-tree `resources/codex` replacement.

## Scope

Included:
- refresh exact `codex-desktop-linux` baseline;
- trace launcher, Desktop-owned app-server spawn, shared socket/remote control/computer-use relevant paths that select Codex runtime;
- determine whether one package-level `CODEX_CLI_PATH` setting covers all required Desktop launch paths;
- identify any hard-coded bundled-path exceptions relevant to normal Desktop operation;
- compare Rank 1 vs Rank 2 against rollback, updater, provenance and fail-closed constraints;
- record one evidence-backed seam decision for M02.

Excluded:
- implementing the feature;
- creating/rebasing `elmakus/codex`;
- building custom Codex;
- production install/runtime mutation.

## Authoritative inputs

- `project-handoffs/M01_HANDOFF.md`
- `implementation/evidence/M01-T03_INTEGRATION_SEAMS.md`
- `implementation/evidence/M01-T05_COMPATIBILITY_GATE.md`
- Master Plan M02
- R1/R2/R3/R6/R7/R10/R11/R12/R13
- current exact `ilysenko/codex-desktop-linux`

## Refresh Gate

Refresh current `codex-desktop-linux:main`, official package tuple and all runtime-selection paths. If upstream has materially changed the feature/launcher/update architecture, block for strategic reconciliation rather than forcing the M01 seam ranking onto new code.

## Expected outputs

- `implementation/evidence/M02-T01_RUNTIME_SELECTION.md`
- explicit decision: `CODEX_CLI_PATH` managed staged runtime OR candidate-tree replacement
- exact source locations and constraints consumed by T02 OpenSpec

## Acceptance criteria

- all relevant normal Desktop Codex/app-server launch paths are traced with current-source evidence;
- the selected seam is justified against official-baseline preservation, feature-off behavior, fail-closed candidate construction and rollback;
- any known path not covered by `CODEX_CLI_PATH` is explicit;
- no implementation begins before the seam decision is durable.

## Required evidence/checks

Current GitHub source references, launcher/feature/update-builder path analysis, and any existing tests proving runtime path selection. No production runtime inspection is required unless source evidence is insufficient and a safe isolated path exists.

## Executor / capability gate

Policy: `chatgpt_only`.

Required capabilities: public/current GitHub repository analysis and durable project writes. Current ChatGPT session can satisfy these; route to ChatGPT when execution starts.

## Git/GitHub state

Project durable state remains on `elmakus/Codex-fix-pooling:main`. External repositories are read-only for this card.

## Blocker/escalation

If neither seam can preserve the accepted fail-closed/package architecture without a new cross-component design, block and escalate architecture instead of inventing a third mechanism silently.

## Result

executor:
result_commit:
result_pr:
evidence:
tests_summary:

## Definition of Done

Acceptance is satisfied, evidence is durable, Task Board/card state is reconciled, exact result pointers are recorded, and T02 has one concrete seam to specify.