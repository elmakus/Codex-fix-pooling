# M01-T01 — Trace official package and bundled Codex baseline

- milestone: M01
- execution_status: done
- executor: chatgpt
- priority: P0
- complexity: medium
- phase: research/discovery
- depends_on: []
- OpenSpec: no

## Goal

Establish the exact current `codex-desktop-linux` baseline and trace how the verified official OpenAI Linux package and its `resources/codex` runtime are discovered, verified, extracted, staged and carried into Community output.

## Scope

Included:
- pin exact audited `ilysenko/codex-desktop-linux` commit at execution time;
- identify official Linux package version, architecture-specific repository path/hash/release identity exposed by current source metadata;
- trace relevant trust/discovery/extraction/package assembly code from official package to Community `resources/codex`;
- determine what metadata/version/build identity can be obtained from the bundled runtime without modifying production state;
- record architecture differences relevant to runtime identity.

Excluded:
- building/replacing Codex;
- changing Community source;
- installing/rebuilding production packages;
- designing the final custom-runtime feature.

## Authoritative inputs

- `PROJECT.md`
- `requirements/REQUIREMENTS.md` R3, R6, R8, R11
- `planning/MASTER_PLAN.md` M01
- current `ilysenko/codex-desktop-linux:main` and its official-package metadata/trust/package scripts

## Refresh Gate

Immediately before execution refresh `ilysenko/codex-desktop-linux:main`, exact official package metadata and relevant paths. If upstream has materially replaced its package trust/acquisition architecture, record evidence and block only if the Master Plan architecture assumptions are invalidated; otherwise reconcile source-path drift inside this card.

## Expected outputs

- `implementation/evidence/M01-T01_BASELINE.md`
- exact Desktop repository SHA and official package identity
- evidence-backed path from verified official package to Community `resources/codex`
- bundled-runtime identity observations usable by T02

## Acceptance criteria

- exact source SHA and official package tuple are recorded;
- trust verification and extraction/staging chain is cited to current source;
- `resources/codex` origin is proven rather than assumed;
- available runtime identity signals are enumerated with commands/source locations or artifact metadata needed to reproduce them;
- no production/runtime state is mutated.

## Required evidence/checks

Current GitHub source references plus, only if needed and safely available, read-only inspection of a downloaded/extracted official package in an isolated workspace. Record hashes/versions and exact commands if artifact inspection occurs.

## Executor / capability gate

Policy is `chatgpt_only`. Required capabilities are public GitHub research and, if source metadata cannot answer runtime identity, safe read-only artifact acquisition/extraction in an isolated workspace. At prep time ChatGPT has the repository research path; execution must re-run the Capability Gate before starting and block rather than use production state if isolated artifact inspection is unavailable.

## Git/GitHub state

Research-only project milestone. Do not modify external source repositories. Durable evidence and task-state updates belong on `elmakus/Codex-fix-pooling:main` unless execution prep is later amended to use a milestone branch.

## Blocker/escalation

If exact official package/runtime identity cannot be obtained without prohibited production mutation, persist what is known and block for a safe artifact/evidence path. Do not infer identity from version similarity.

## Result

executor: chatgpt
result_commit: 4af4ccc9b167e79cf32813deae9ed5ee07abfd8f
result_pr: null
evidence: implementation/evidence/M01-T01_BASELINE.md
tests_summary: Verified current `codex-desktop-linux` main SHA; pinned both architecture package tuples; traced signed APT trust, extraction, whole-tree staging and package layout; documented reproducible isolated runtime identity commands. An isolated artifact download was attempted but DNS to `persistent.oaistatic.com` was unavailable; no production/runtime state was touched and no source-revision inference was made.

## Definition of Done

Acceptance and evidence above are satisfied, Task Board/card state is reconciled, exact result pointers are recorded, and T02/T03 receive an unambiguous baseline identity.
