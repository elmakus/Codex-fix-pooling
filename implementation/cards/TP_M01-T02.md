# TP_M01-T02 — Issue strict upstream pruning-equivalence verdict

- Decision state: `ACCEPTED`
- Execution status: `DONE`
- Executor: `chatgpt`
- Milestone: `TP_M01`
- Priority: `HIGH`
- Complexity: `HIGH`
- Phase: `research/discovery`

## Dependencies

- `TP_M01-T01`

## Expected code locations

- current pinned `openai/codex` request/context/tool-output/compaction paths identified by T01
- reference patch `tekacs/codex@d70b903a4edbbb02c5009ae8e6194f2128d80213`

## Canonical sources

- Requirements: `R3-R11`, `R19`, `requirements/REQUIREMENTS.md`
- Master Plan: TP_M01 upstream-equivalence gate
- Research: `research/tool-output-pruning.md`
- T01 evidence/result

## OpenSpec

- Required/candidate: `skip`
- Change: none

## Outcome

Determine whether official Codex on the exact T01 baseline already provides behavior functionally equivalent to the historical pruning patch, regardless of implementation name or structure.

## Scope

### Included

Compare current upstream behavior against the reference semantics:

- request-time context reduction of old tool outputs;
- recent-output protection;
- minimum-benefit/no-op behavior;
- failed-output protection;
- patch/durability-sensitive output protection;
- standard/custom output coverage where applicable;
- non-destructive canonical-history behavior;
- feature/control semantics where materially equivalent.

Issue one verdict: `EQUIVALENT`, `NOT_EQUIVALENT`, or `UNKNOWN`.

### Excluded

- implementing missing behavior;
- changing thresholds;
- Desktop packaging/integration;
- deciding final TP_M02 implementation mechanics beyond implications of the verdict.

## Constraints

- Exact source marker absence is not sufficient for `NOT_EQUIVALENT`.
- Equivalent high-level compaction is not sufficient unless it covers the pruning objective and safety semantics relevant to this project.
- `UNKNOWN` blocks custom implementation selection until resolved.

## Acceptance

- each reference behavior dimension has current-baseline evidence;
- verdict is explicit and justified;
- if `EQUIVALENT`, evidence is strong enough to support retiring custom pruning for this baseline;
- if `NOT_EQUIVALENT`, missing semantics are enumerated;
- if `UNKNOWN`, exact unresolved evidence gap is durable.

## Required tests / checks

- source comparison against exact refs;
- verify all required semantic dimensions are addressed;
- independent consistency check against T01 architecture map.

## External write/readback needs

Only durable GitHub project-state/evidence writes; read back written artifact/commit.

## Independent review

`REQUIRED` — this verdict determines whether custom divergence is justified at all.

## Refresh Gate

Refresh exact upstream baseline before analysis. If T01's baseline changed materially, reconcile T01 evidence first rather than issuing a verdict on mixed baselines.

Refresh Gate 2026-09-12: GREEN. `openai/codex:main` remained `944d6fd1ba4baab69dbedd205282dc72ec20abb5`, exactly matching T01 evidence. T01 was DONE with durable evidence and no implementation/runtime mutation. T02 was the sole dependency-eligible next card.

## Result

- result_commit: `7fc54bbcd9b87a47a5e80af6aae5ac93d0a23189`
- result_pr: `null`
- evidence: `implementation/evidence/TP_M01-T02_PRUNING_EQUIVALENCE.md`
- tests_summary: Exact upstream ref refreshed; historical patch diff re-read; all required semantic dimensions compared against current per-tool truncation, prompt normalization, context-window recovery and compaction paths; second-pass consistency review against T01 completed; verdict `NOT_EQUIVALENT`; no runtime/source mutation performed.

## Definition of Done

- [x] Included scope complete
- [x] Acceptance satisfied
- [x] Required checks executed
- [x] Checks green or authorized exception recorded
- [x] No hidden blocker
- [x] Result durable in Git
- [x] Task Board reconciled
- [x] Executor/result pointers recorded
- [x] Evidence identifies exact verification
- [x] No unassigned TODO in accepted scope
