# M02-T03 — Establish controlled Codex carrier and reproducible runtime build provenance

- milestone: M02
- execution_status: blocked
- executor: null
- priority: P0
- complexity: high
- phase: implementation
- depends_on: [M02-T02]
- OpenSpec: `openspec/changes/custom-codex-runtime-m02`

## Goal

Establish the controlled `elmakus/codex` patch carrier and a reproducible build/provenance path that can produce a candidate runtime bound to an exact OpenAI upstream base and patch revision without modifying production state.

## Scope

Included:
- create/use owned `elmakus/codex` fork only when execution reaches this card;
- pin exact upstream OpenAI base selected under the M01/M02 contract;
- carry the reference wakeup change or documented equivalent change set;
- record upstream base SHA, carrier SHA, patch provenance, target architecture, build command/toolchain and binary identity;
- produce an isolated candidate artifact suitable for later package integration.

Excluded:
- production install;
- exact Desktop compatibility admission;
- M03 behavioral closeout beyond build-time/unit checks needed to establish a valid candidate;
- update refresh across a second official Desktop baseline.

## Authoritative inputs

- `openspec/changes/custom-codex-runtime-m02`
- M01-T04 refresh/carrier decision
- M01-T05 provenance gate
- current `openai/codex` and `tekacs/codex@9ffcf8db9078eae43d4111ff94259795c1e962c9`

## Refresh Gate

Refreshed before any mutation:
- `openai/codex:main` = `944d6fd1ba4baab69dbedd205282dc72ec20abb5`;
- `ilysenko/codex-desktop-linux:main` = `249cd4b64d42434f51417fec4a318750d461b676`;
- official Linux Desktop baseline remains `26.908.40834`;
- reference patch remains `tekacs/codex@9ffcf8db9078eae43d4111ff94259795c1e962c9`;
- no existing `elmakus/codex` carrier was found.

No material architecture drift was found.

## Expected outputs

- controlled owned carrier state with exact Git provenance;
- reproducible isolated build procedure;
- candidate binary identity (`--version`, SHA-256, architecture);
- durable evidence under `implementation/evidence/`.

## Acceptance criteria

- no stale historical binary is reused;
- exact upstream base and patch/carrier commits are recorded;
- candidate build succeeds in isolation;
- resulting binary identity is durable and corresponds to recorded source;
- production Community install remains untouched.

## Required evidence/checks

Git refs/commits, build log/commands, toolchain identity, binary SHA-256/version, and relevant unit/build checks. Material external GitHub writes require readback verification.

## Required capabilities

- GitHub repository/fork/branch write capability for `elmakus/codex`;
- isolated source build environment capable of building target Codex runtime and obtaining binary identity;
- durable project evidence writes.

## Executor / capability gate

Policy: `chatgpt_only`.

Capability Gate result: **BLOCKED before assignment**.

Missing capabilities in the current normal ChatGPT session:
- no repository creation/fork action is exposed to provision the absent `elmakus/codex` carrier;
- the isolated runtime has no `rustc` or `cargo`, so the required Codex source build, tests and binary identity cannot be produced.

Durable evidence: `implementation/evidence/M02-T03_CAPABILITY_BLOCKER.md`.

## Git/GitHub state

Project truth remains in `elmakus/Codex-fix-pooling`. No external carrier mutation occurred.

## Blocker/escalation

Resume this same card only after both requirements are available:
1. the owned `elmakus/codex` carrier exists with connector write access, or the current ChatGPT GitHub capability can create/fork it;
2. an isolated Rust/Cargo build environment is available for the required build/tests/binary identity.

Under `chatgpt_only`, do not route to Codex automatically.

## Result

executor: null
result_commit: null
result_pr: null
evidence: implementation/evidence/M02-T03_CAPABILITY_BLOCKER.md
tests_summary: Not run; Capability Gate failed before assignment because required carrier-provisioning and Rust/Cargo build capabilities are unavailable.

## Definition of Done

Not satisfied. Card is durably BLOCKED before assignment; no implementation or production mutation has begun.