# TP_M01-T01 — Pin baselines and map current request/context architecture

- Decision state: `ACCEPTED`
- Execution status: `READY`
- Executor: `null`
- Milestone: `TP_M01`
- Priority: `HIGH`
- Complexity: `MEDIUM`
- Phase: `research/discovery`

## Dependencies

- none

## Expected code locations

Research/read-only inspection of current upstream source, especially:

- `openai/codex` request construction / sampling path
- context manager/history/tool-output accounting
- compaction/truncation/token-budget code
- `ilysenko/codex-desktop-linux` official package/runtime acquisition and identity seams

## Canonical sources

- Requirements: `R3-R8`, `R11-R17`, `requirements/REQUIREMENTS.md`
- Master Plan: `planning/MASTER_PLAN.md` sections 2, 6, 7, TP_M01
- Research: `research/tool-output-pruning.md`
- Latest cumulative handoff: none

## OpenSpec

- Required/candidate: `skip`
- Change: none

## Outcome

Produce a durable, exact baseline record and an evidence-backed map of how current Codex constructs model request context and represents/handles tool outputs before any pruning-equivalence judgment is attempted.

## Scope

### Included

- pin exact current `openai/codex` commit;
- pin exact current `ilysenko/codex-desktop-linux` commit and official package identity it targets;
- determine the strongest available identity for bundled Codex runtime and source mapping;
- map current sampling/request construction path;
- map current context manager/history handling;
- map standard/custom tool output item representations still relevant to pruning;
- map current compaction, truncation and token-budget mechanisms that could overlap pruning;
- record uncertainty explicitly where source/runtime mapping is not deterministic.

### Excluded

- pruning implementation;
- cherry-pick/rebase of `d70b903`;
- creating or modifying an `elmakus/codex` fork;
- production/runtime/package mutation;
- final upstream-equivalence verdict (T02).

## Constraints

- Use current public source/evidence, not stale chat memory.
- Identity ambiguity is recorded as `UNKNOWN`, never guessed.
- Do not assume old historical source seams still exist.

## Acceptance

- exact source/Desktop/package baselines are recorded with SHAs/identities;
- bundled runtime/source mapping has an explicit deterministic/safely-derived/unavailable verdict;
- current request/context/tool-output architecture is documented with source pointers;
- current compaction/truncation mechanisms relevant to pruning are identified;
- T02 has sufficient authoritative inputs for strict equivalence analysis.

## Required tests / checks

- source-path/readback checks against exact pinned refs;
- cross-check that cited files/paths exist at pinned refs;
- verify no implementation/runtime mutation occurred.

## External write/readback needs

Only durable GitHub project-state/evidence writes; read back written artifact/commit.

## Independent review

`RECOMMENDED` — architecture mapping errors would contaminate all later pruning decisions.

## Refresh Gate

Before execution compare actual `tool-output-pruning` HEAD, current upstream refs, Master Plan/requirements and source interfaces. If upstream moved, pin the new exact baseline and continue. If current architecture invalidates the approved non-destructive/request-time intent, block for strategic review instead of silently redefining it.

## Result

- result_commit:
- result_pr:
- evidence:
- tests_summary:

## Definition of Done

- [ ] Included scope complete
- [ ] Acceptance satisfied
- [ ] Required checks executed
- [ ] Checks green or authorized exception recorded
- [ ] No hidden blocker
- [ ] Result durable in Git
- [ ] Task Board reconciled
- [ ] Executor/result pointers recorded
- [ ] Evidence identifies exact verification
- [ ] No unassigned TODO in accepted scope
