# M02-T05 — Verify M02 package-level acceptance and rollback readiness

- milestone: M02
- execution_status: planned
- executor: null
- priority: P0
- complexity: high
- phase: verification
- depends_on: [M02-T04]
- OpenSpec: yes

## Goal

Run the M02 package-level acceptance matrix against the implemented feature/build path without claiming M03/M04/M05 completion and without installing into production.

## Scope

Included:
- stock feature-off package behavior;
- controlled PASS fixture/candidate selection;
- FAIL and UNKNOWN rejection behavior;
- provenance/build metadata readback;
- package candidate construction failure safety;
- stock rebuild restoration path;
- previous managed package rollback readiness evidence where available without production mutation;
- OpenSpec verification for all M02-owned behavior.

Excluded:
- M03 full wakeup-semantic acceptance;
- M04 exact Desktop GUI/app-server end-to-end acceptance;
- M05 real changed-official-baseline update persistence test;
- production installation.

## Authoritative inputs

- active M02 OpenSpec
- M02-T04 implementation/result evidence
- M01-T05 minimum downstream matrix
- requirements R1/R2/R3/R6/R7/R8/R10/R11/R12/R13

## Refresh Gate

Verify exact implementation branch/HEAD, candidate provenance, current official package tuple, OpenSpec state and test commands. Material implementation drift from the frozen contract blocks acceptance until reconciled.

## Expected outputs

- `implementation/evidence/M02_ACCEPTANCE_PREP.md` or equivalent card evidence;
- exact test/check results for M02-owned behavior;
- explicit remaining boundaries for M03/M04/M05;
- milestone-ready state if all checks are green.

## Acceptance criteria

- feature disabled => stock runtime selection;
- controlled PASS => exact expected managed custom runtime selected;
- FAIL/UNKNOWN => custom runtime not selected and candidate fails safely;
- source/upstream/patch/carrier/binary provenance is readable and exact;
- official package trust path remains intact;
- feature-off rebuild path restores stock selection;
- production install remains untouched;
- no hidden claim of exact Desktop compatibility or wakeup semantics is made before later milestones.

## Required evidence/checks

Exact commands/tests, package/candidate metadata readback, generated artifact identity, negative-path assertions, OpenSpec verification and external branch/commit readback.

## Required capabilities

- isolated Community build/test environment;
- access to exact T04 implementation branch and candidate artifact;
- ability to inspect produced package artifacts/metadata;
- durable project evidence writes.

## Executor / capability gate

Policy: `chatgpt_only`. Missing isolated build/test or artifact-inspection capability blocks the card under current policy.

## Git/GitHub state

Acceptance runs on the intended final M02 implementation branch state. Do not merge/upstream or deploy merely because this card becomes green. Milestone integrated review follows separately.

## Blocker/escalation

Any custom selection on FAIL/UNKNOWN, loss of stock restoration, provenance mismatch, or package trust regression is RED and requires corrective work before milestone review.

## Result

executor:
result_commit:
result_pr:
evidence:
tests_summary:

## Definition of Done

M02-owned acceptance is green, OpenSpec agrees with implementation, exact result pointers/evidence are durable, Task Board/card state is reconciled, and the milestone is ready for integrated acceptance review.