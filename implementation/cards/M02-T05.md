# M02-T05 — Assemble Codex MVP handoff package

- milestone: M02
- execution_status: in_progress
- executor: chatgpt
- priority: P0
- complexity: high
- phase: handoff
- depends_on: [M02-T04]
- OpenSpec: yes

## Goal

Assemble one deterministic best-effort handoff for Codex containing the runtime patch/provenance recipe, Community integration patch, exact apply/build/test/fix instructions, frozen invariants, and all known UNKNOWNs.

## Scope

Included:
- collect T03 runtime patch/provenance output;
- collect T04 Community feature patch;
- identify exact upstream/base commits targeted by both;
- provide ordered Codex execution steps: inspect → refresh → apply/adapt → build runtime → record identity → apply/build Community → run gates/tests → repair concrete incompatibilities → report unresolved evidence;
- provide acceptance checklist spanning M02 package mechanics plus later wakeup/Desktop/update checks that Codex should attempt where environment permits;
- explicitly separate what ChatGPT prepared from what Codex must prove;
- preserve production no-touch boundary unless user separately authorizes installation.

Excluded:
- claiming M02 package acceptance was executed locally;
- claiming M03/M04/M05 GREEN before Codex evidence exists;
- production deployment authorization.

## Authoritative inputs

- `planning/M02_BEST_EFFORT_MVP_PIVOT.md`
- active M02 OpenSpec
- M02-T03 output/evidence
- M02-T04 output/evidence
- M01-T05 compatibility matrix

## Expected outputs

- a single durable Codex handoff document;
- exact patch/change-set pointers;
- exact known-UNKNOWN list;
- exact command/test checklist for Codex;
- recovery instructions: if application/build fails, preserve symptoms/logs and repair against frozen invariants rather than discarding the design by default.

## Acceptance criteria

- Codex can begin from the handoff without redoing M01/T01/T02 architecture research;
- every patch has an exact target baseline and provenance pointer;
- no local unverified build/test is represented as PASS;
- FAIL/UNKNOWN remain fail-closed for compatibility claims;
- handoff distinguishes build mechanics, wakeup semantics, Desktop integration and updater persistence checks;
- production install remains unauthorized unless the user explicitly changes that boundary.

## Required evidence/checks

Cross-check patch pointers, baseline SHAs, invariant list, OpenSpec mapping, execution ordering, and known UNKNOWNs. Source-level consistency/readback is required; environment-dependent build/test results are deferred to Codex.

## Required capabilities

- project repository read/write capability;
- ability to inspect the prepared T03/T04 artifacts and source evidence.

## Executor / capability gate

Policy: `mixed`. ChatGPT owns handoff assembly. After this card, Codex is the intended executor for the environment-dependent continuation explicitly authorized by the user.

## Result

executor:
result_commit:
result_pr:
evidence:
tests_summary:

## Definition of Done

One coherent MVP package is durably ready for Codex, all non-claims are explicit, production state is untouched, and milestone checkpoint `M02_MVP_HANDOFF_READY` can be evaluated.