# M01-T04 — Determine upstream equivalence and patch-carrier refresh strategy

- milestone: M01
- execution_status: planned
- executor: null
- priority: P0
- complexity: high
- phase: research/discovery
- depends_on: [M01-T02]
- OpenSpec: no

## Goal

Give a strict equivalence verdict for the reference wakeup behavior against the exact baseline and define how divergence is refreshed or retired on each future official baseline.

## Scope

Included: compare `tekacs/codex@9ffcf8d` semantics/tests with exact mapped/current OpenAI source; inspect idle wake, active-turn pushed input, duplicate suppression and tool guidance; distinguish similar telemetry/background completion work from model wake behavior; define refresh order; confirm patch carrier choice.

Excluded: fork creation, cherry-pick, rebase, custom build, implementation.

## Authoritative inputs

T02 mapping/protocol evidence; reference patch commit/diff; current exact `openai/codex` baseline; R4/R8/R9/R14; Master Plan A5/A7.

## Refresh Gate

Refresh `openai/codex` immediately before analysis. If upstream moved after T02, preserve the exact official bundled baseline as primary and separately record current-main retirement evidence; do not conflate them.

## Expected outputs

`implementation/evidence/M01-T04_EQUIVALENCE_REFRESH.md` with four-part equivalence matrix, verdict, patch-refresh algorithm and patch-carrier decision.

## Acceptance criteria

- each R4 semantic has explicit evidence and verdict;
- equivalent-looking background metrics/events are not treated as model-visible wake without proof;
- overall verdict is `equivalent`, `not equivalent`, or `unknown`; `unknown` cannot retire the patch;
- refresh lifecycle is upstream-equivalence check → baseline selection → patch application/rebase if needed → tests/gates;
- `elmakus/codex` is confirmed as carrier or an evidence-backed alternative is recorded.

## Required evidence/checks

Reference patch diff/tests, exact upstream source/tests/commit history, and where feasible source-level behavioral test correspondence. No production runtime execution.

## Executor

`chatgpt_only`; executor null until start. No OpenSpec.

## Git/GitHub state

Project `main`; `tekacs/codex`, `openai/codex`, and any prospective fork remain read-only.

## Blocker/escalation

If equivalence cannot be proven, retain divergence requirement; do not retire patch. A change away from controlled fail-closed refresh or owned carrier preference requires strategic evidence/decision.

## Result

executor:
result_commit:
result_pr:
evidence:
tests_summary:

## Definition of Done

Strict equivalence and refresh/carrier results are durable and consumable by T05.
