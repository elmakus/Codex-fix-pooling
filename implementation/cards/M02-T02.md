# M02-T02 — Freeze feature/settings/provenance contract in OpenSpec

- milestone: M02
- execution_status: done
- executor: chatgpt
- priority: P0
- complexity: high
- phase: design/specification
- depends_on: [M02-T01]
- OpenSpec: `openspec/changes/custom-codex-runtime-m02`

## Goal

Create the just-in-time OpenSpec contract for the concrete `custom-codex-runtime` feature after T01 has proven the substitution seam.

## Scope

Included:
- feature enable/disable semantics;
- settings schema for source/provider/ref/build inputs required by the selected design;
- official baseline identity inputs;
- custom runtime provenance fields;
- PASS/FAIL/UNKNOWN gate semantics and rejection behavior;
- stock restoration behavior;
- package/update-builder integration contract relevant to M02;
- diagnostic evidence contract.

Excluded:
- implementing feature code;
- M03 wakeup-semantic proof;
- M04 exact Desktop integration acceptance;
- M05 changed-baseline update-path acceptance.

## Authoritative inputs

- M02-T01 evidence
- M01 compatibility gate and handoff
- requirements R1/R2/R3/R6/R7/R8/R10/R11/R12/R13
- current Linux feature framework contracts
- workflow OpenSpec contract

## Refresh Gate

Reconcile the proposed OpenSpec against the exact seam selected by T01 and current feature/update-builder interfaces. Material contradiction with accepted architecture blocks rather than being hidden in spec wording.

Refresh result: the Task Board correctly marked this card `ready` while the card file still said `planned`; this local durable-state mismatch was reconciled at execution start. M02-T01 froze candidate-tree replacement of `resources/codex`, and no material architecture contradiction was found.

## Expected outputs

- one active OpenSpec change under `openspec/changes/` covering the M02 feature/settings/provenance/package contract;
- explicit implementation tasks inside the OpenSpec change mapped to M02-T03/T04/T05;
- any durable design decision required by the selected seam captured in the OpenSpec design.

## Acceptance criteria

- configuration and provenance fields are precise enough to test;
- enabled/disabled behavior is unambiguous;
- FAIL and UNKNOWN both prohibit custom-runtime selection;
- official package verification remains prerequisite and is not weakened;
- stock restoration/rollback behavior is explicit;
- downstream M03-M05 responsibilities are not accidentally absorbed into M02.

## Required evidence/checks

OpenSpec consistency review against M01 evidence, Master Plan, requirements and current source interfaces.

## Executor / capability gate

Policy: `chatgpt_only`. Repository/specification work executed in ChatGPT.

## Git/GitHub state

Project `main`; external repositories remained read-only for this card.

## Blocker/escalation

If the selected seam cannot be specified without changing frozen architecture or compatibility semantics, block for strategic resolution.

## Result

executor: chatgpt
result_commit: 818688cd49058d5b52fdfdf3d2ddcc0524a7d746
result_pr: null
evidence: implementation/evidence/M02-T02_OPENSPEC.md
tests_summary: OpenSpec consistency review GREEN against M02-T01 seam, requirements, Master Plan and workflow OpenSpec contract; no implementation or production mutation performed.

## Definition of Done

GREEN. OpenSpec is coherent and durable, accepted scope is mapped to M02-T03/T04/T05, Task Board/card state is reconciled, and no implementation began prematurely.