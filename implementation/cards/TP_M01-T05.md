# TP_M01-T05 — Select TP_M02 strategy and refresh runtime-delivery compatibility assumptions

- Decision state: `ACCEPTED`
- Execution status: `DONE`
- Executor: `chatgpt`
- Milestone: `TP_M01`
- Priority: `HIGH`
- Complexity: `HIGH`
- Phase: `research/synthesis`

## Dependencies

- `TP_M01-T02`
- `TP_M01-T03`
- `TP_M01-T04`

## Expected code locations

Read-only synthesis across:

- current pinned `openai/codex` architecture;
- current `ilysenko/codex-desktop-linux` custom-runtime/feature/update seams;
- sibling wakeup workstream research only where still current and independently verified.

## Canonical sources

- Requirements: `R1`, `R2`, `R11-R18`, `R22`, `R23`
- Master Plan: TP_M01 outcome/acceptance; A7-A9
- T02/T03/T04 evidence
- shared architectural research only after refresh

## OpenSpec

- Required/candidate: `skip` for this discovery card; re-evaluate before TP_M02
- Change: none

## Outcome

Close TP_M01 with one evidence-backed next strategy: `NO_CUSTOM_PATCH`, `DIRECT/ADAPTED_REBASE`, `REIMPLEMENT_EQUIVALENT_BEHAVIOR`, or an explicit blocker. Refresh the Community custom-runtime compatibility/delivery assumptions needed by TP_M02 without implementing them.

## Scope

### Included

- synthesize T02 equivalence verdict, T03 safety contract and T04 benchmark contract;
- select or block the TP_M02 implementation path;
- refresh current Community `resources/codex`, feature framework, `CODEX_CLI_PATH`/runtime selection and updater persistence assumptions relevant to pruning;
- define compatibility-gate inputs TP_M02/TP_M04 must later satisfy;
- decide whether an owned patch carrier remains preferred if custom divergence is needed;
- identify just-in-time OpenSpec candidates for TP_M02;
- produce TP_M01 integrated acceptance evidence inputs.

### Excluded

- implementing pruning;
- modifying/forking/rebasing Codex;
- implementing Community feature/package changes;
- production/runtime mutation.

## Constraints

- `UNKNOWN` equivalence or unresolved safety blocks custom implementation selection.
- Do not reuse stale sibling-workstream conclusions without refreshing exact current refs.
- Avoid a second package replacement mechanism unless current evidence proves the generic runtime boundary unsuitable.

## Acceptance

- exactly one TP_M02 path or explicit blocker is recorded;
- custom divergence is rejected if T02 proves current upstream equivalent;
- Community delivery/compatibility assumptions are refreshed against exact current source;
- compatibility gate inputs and fail-closed semantics are explicit;
- patch-carrier recommendation is explicit if custom code remains needed;
- OpenSpec JIT decision for TP_M02 is stated;
- TP_M01 integrated acceptance can be evaluated without implementation.

## Required tests / checks

- dependency evidence completeness check;
- current Community source/readback checks for every reused delivery assumption;
- requirement coverage and TP_M01 integrated acceptance review.

## External write/readback needs

Only durable GitHub project-state/evidence writes; read back written artifact/commit.

## Independent review

`REQUIRED` — this synthesis controls whether the project carries custom Codex divergence and what TP_M02 is allowed to implement.

## Refresh Gate

Refresh exact upstream/Community refs and all dependency evidence. Material contradiction with frozen architecture or requirements blocks strategic completion rather than being silently reconciled.

Refresh Gate 2026-09-12: GREEN. `openai/codex:main` remained `944d6fd1ba4baab69dbedd205282dc72ec20abb5`; `ilysenko/codex-desktop-linux:main` remained `249cd4b64d42434f51417fec4a318750d461b676`; T02 is `NOT_EQUIVALENT`; T03 safety contract and T04 benchmark contract are durable. Current Community source independently confirms verified official-package `resources/codex`, opt-in Linux feature persistence across updater rebuilds, and an explicit `CODEX_CLI_PATH` override seam. No contradiction invalidates A7-A9.

## Result

- result_commit: `1893b2c2631e571bf376f0da7f39778842259406`
- result_pr: `null`
- evidence: `implementation/evidence/TP_M01-T05_STRATEGY_AND_COMPATIBILITY.md`
- tests_summary: T02/T03/T04 dependency evidence complete; exact upstream/Community refs refreshed; official-package/bundled-runtime, Linux feature, updater persistence and `CODEX_CLI_PATH` assumptions independently re-read from current Community source; strategy selected as `REIMPLEMENT_EQUIVALENT_BEHAVIOR`; compatibility/fail-closed inputs and JIT OpenSpec requirement defined; no fork/package/runtime mutation.

## Definition of Done

- [x] Included scope complete
- [x] Acceptance satisfied
- [x] Required checks executed
- [x] Checks green or authorized exception recorded
- [x] No hidden blocker in card scope
- [x] Result durable in Git
- [x] Task Board reconciled
- [x] Executor/result pointers recorded
- [x] Evidence identifies exact verification
- [x] No unassigned TODO in accepted scope

Milestone terminal acceptance remains pending the workflow-required fresh independent review for cards marked `Independent review: REQUIRED` and for final TP_M01 acceptance.
