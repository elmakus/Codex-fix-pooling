# M02-T04 — Prepare best-effort custom-codex-runtime Community patch

- milestone: M02
- execution_status: done
- executor: chatgpt
- priority: P0
- complexity: high
- phase: implementation-prep
- depends_on: [M02-T02, M02-T03]
- OpenSpec: yes

## Goal

Prepare a source-level MVP patch for `custom-codex-runtime` against the refreshed Community Edition source, using the seam frozen by T01/T02, without pretending that package build/test acceptance has already passed.

## Scope

Included:
- feature manifest/settings implementation patch;
- official-baseline identity capture before substitution;
- candidate-tree `resources/codex` replacement path;
- PASS/FAIL/UNKNOWN gate plumbing at source level;
- provenance metadata/diagnostics scaffolding;
- feature-off stock reconstruction path;
- integration with existing feature/update-builder mechanics;
- static/source-level review against the exact refreshed Community baseline;
- explicit notes for any API/build details that Codex must reconcile in its environment.

Excluded:
- production install/deploy;
- claiming a successful Community package build in ChatGPT;
- claiming runtime compatibility PASS before Codex runs the gates;
- M03 wakeup-behavior acceptance;
- M04 Desktop end-to-end closeout;
- M05 changed-baseline acceptance.

## Authoritative inputs

- `planning/M02_BEST_EFFORT_MVP_PIVOT.md`
- active M02 OpenSpec
- T01 seam decision
- T03 runtime patch/provenance bundle
- M01-T05 compatibility gate
- refreshed `ilysenko/codex-desktop-linux` source

## Refresh Gate

Refresh Community source/branch, feature framework and update-builder before preparing the patch. Preserve the frozen candidate-tree substitution seam unless current source proves it invalid.

## Expected outputs

- durable best-effort Community patch/change set;
- exact base commit it targets;
- source-level implementation notes;
- explicit unverified build/runtime assumptions;
- deterministic instructions Codex can use to apply/adapt/build/test it.

## Acceptance criteria

- source patch preserves official package verification as prerequisite;
- disabled feature path reconstructs stock `resources/codex`;
- source logic does not authorize custom selection on FAIL/UNKNOWN;
- custom replacement is confined to candidate construction, not live production overwrite;
- provenance/diagnostic fields required by OpenSpec are represented or clearly marked for Codex completion where runtime-generated values are impossible to know beforehand;
- existing updater/build path is reused rather than replaced;
- all unverified behavior is explicitly UNKNOWN.

## Required evidence/checks

Exact target commit, changed files/diff, source-level consistency checks, mapping to OpenSpec requirements, known UNKNOWNs, and Codex execution checklist.

## Required capabilities

- read/write access to project repository;
- read access to refreshed Community source;
- source-level patch/diff preparation capability.

An isolated Community package build environment is not required for this preparation card; final build/test responsibility is transferred in T05 handoff.

## Executor / capability gate

Policy: `mixed`. ChatGPT should prepare the patch when source-level capabilities suffice. Codex is the intended executor for apply/adapt/build/debug/verify after handoff.

## Git/GitHub state

Do not modify `ilysenko/codex-desktop-linux` upstream directly. The deliverable may be a durable patch against that baseline or changes intended for an owned development fork/branch that Codex creates/uses during execution.

## Blocker/escalation

Block if source drift invalidates the selected substitution seam or if implementation would require weakening official-package trust or fail-closed semantics.

## Result

executor: chatgpt
result_commit: f84122e56361e262704b0b05704e5ce0166b5c59
result_pr: null
evidence: implementation/evidence/M02-T04_COMMUNITY_PATCH_PREPARATION.md
tests_summary: PREPARED, NOT EXECUTED; exact Community test/package lanes remain UNKNOWN for Codex

## Definition of Done

The Community side of the MVP is source-complete enough for Codex to inspect, apply/adapt, build and verify, with exact base/provenance and explicit UNKNOWNs.