# M02-T04 — Implement custom-codex-runtime feature and fail-closed package selection

- milestone: M02
- execution_status: planned
- executor: null
- priority: P0
- complexity: high
- phase: implementation
- depends_on: [M02-T02, M02-T03]
- OpenSpec: yes

## Goal

Implement the `custom-codex-runtime` feature on the selected Community development branch/fork using the seam frozen by T01/T02, integrating candidate runtime selection, provenance metadata and fail-closed rejection into managed package construction.

## Scope

Included:
- feature manifest/settings implementation;
- candidate runtime staging/acquisition from the controlled T03 output;
- official-baseline identity capture before substitution;
- PASS-only custom selection;
- FAIL/UNKNOWN rejection path;
- build/package provenance metadata;
- feature-off stock restoration;
- package/update-builder snapshot integration required for M02.

Excluded:
- production install/deploy;
- full M03 wakeup-behavior acceptance;
- M04 end-to-end Desktop compatibility closeout;
- M05 changed-baseline updater acceptance.

## Authoritative inputs

- active M02 OpenSpec
- T01 seam decision
- T03 candidate/provenance evidence
- M01-T05 compatibility gate
- current exact `ilysenko/codex-desktop-linux` source

## Refresh Gate

Refresh Community source/branch, feature framework, update-builder and candidate provenance. Any material drift affecting the frozen seam or OpenSpec blocks/reconciles before implementation. Do not weaken PASS/FAIL/UNKNOWN semantics to accommodate drift.

## Expected outputs

- bounded implementation on an isolated development branch/fork;
- feature disabled by default;
- custom-runtime selection only through the managed package build path;
- durable provenance metadata and diagnostics;
- tests/fixtures proving feature selection mechanics and fail-closed behavior.

## Acceptance criteria

- disabled feature leaves stock `resources/codex` behavior intact;
- enabled feature cannot select custom runtime on FAIL or UNKNOWN;
- controlled PASS fixture/path selects the exact expected candidate runtime;
- official package verification/extraction remains prerequisite;
- source/ref/base/patch/binary identity is recorded in generated evidence/build metadata;
- failed candidate validation does not mutate installed production state;
- no post-install overwrite mechanism is introduced.

## Required evidence/checks

Feature-framework tests, package/build tests, PASS/FAIL/UNKNOWN fixtures, generated metadata inspection, stock-off-path verification, and readback of external GitHub branch/commit state.

## Required capabilities

- write access to the authorized Community development fork/branch;
- isolated build/test environment for Community packaging/tests;
- access to T03 candidate artifact or reproducible build path;
- durable project evidence writes.

## Executor / capability gate

Policy: `chatgpt_only`. At execution start, ChatGPT must demonstrate the full repo/build/test capability set. Missing capability blocks the same card under current policy.

## Git/GitHub state

Do not modify `ilysenko/codex-desktop-linux` upstream directly. Use an authorized owned fork/development branch. Record exact branch, HEAD, PR state if any, and project evidence pointers.

## Blocker/escalation

If the selected seam cannot be implemented without weakening official-package trust, fail-closed semantics, or atomic package behavior, block for strategic resolution.

## Result

executor:
result_commit:
result_pr:
evidence:
tests_summary:

## Definition of Done

Implementation scope is complete, applicable OpenSpec tasks/requirements are satisfied, required tests are green, external writes are read back, project Task Board/card result pointers are durable, and no production installation occurred.