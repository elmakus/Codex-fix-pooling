# M01-T03 — Audit runtime override, feature, and update persistence seams

- milestone: M01
- execution_status: in_progress
- executor: chatgpt
- priority: P1
- complexity: medium
- phase: research/discovery
- depends_on: [M01-T01]
- OpenSpec: no

## Goal

Establish current source contracts around `CODEX_CLI_PATH`, Linux features, package assembly, packaged update-builder, `codex-update-manager`, promotion and rollback so M02 can choose a minimal integration seam without guessing.

## Scope

Included: default runtime selection; explicit `CODEX_CLI_PATH`; feature descriptor/settings/staged-resource hooks; enabled-feature snapshot persistence; update candidate rebuild; feature-drift rejection; promotion/rollback constraints.

Excluded: choosing/implementing final M02 feature schema; modifying Community/updater.

## Authoritative inputs

T01 baseline; current `ilysenko/codex-desktop-linux` source/docs/tests; R1-R3, R5-R7, R10, R12-R13, R16; Master Plan M01/M02/M05 boundaries.

## Refresh Gate

Refresh exact current source paths and updater semantics. Source code/tests outrank stale docs where they disagree. Material replacement of feature/update architecture triggers strategic review only if it invalidates frozen A1/A3/A6.

## Expected outputs

`implementation/evidence/M01-T03_INTEGRATION_SEAMS.md` with runtime-selection precedence, candidate integration seams, update-persistence flow, fail-closed insertion points and rollback constraints.

## Acceptance criteria

- default and explicit CLI path behavior is evidence-backed;
- enabled feature/settings persistence through update-builder is traced;
- update trust/build/promotion/rollback lifecycle is documented;
- later substitution points are ranked by fit without prematurely freezing M02 design;
- no second updater or post-install mutation path is proposed without evidence of necessity.

## Required evidence/checks

Current source/docs/tests for Linux features, launcher/runtime hooks, package-common/update-builder and updater state machine.

## Executor

`chatgpt_only`; executor null until start. No OpenSpec.

## Git/GitHub state

Project `main`; external repositories read-only.

## Blocker/escalation

If no package-level seam can preserve verified upstream plus fail-closed update behavior, persist evidence and block M02 architecture rather than inventing a post-install workaround.

## Result

executor:
result_commit:
result_pr:
evidence:
tests_summary:

## Definition of Done

Acceptance/evidence complete, result pointers durable, and T05 has verified update/runtime-selection constraints.
